type: object
description: |
  Bulk response containing ETIM modelling class-feature relations with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM modelling class-feature relations.
    items:
      $ref: ../domain/EtimModellingClassFeature.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml