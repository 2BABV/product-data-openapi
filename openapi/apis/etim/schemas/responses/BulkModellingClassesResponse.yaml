type: object
description: |
  Bulk response containing ETIM modelling classes with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM modelling classes.
    items:
      $ref: ../domain/EtimModellingClass.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml