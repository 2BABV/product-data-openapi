type: object
description: |
  Bulk response containing ETIM feature groups with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM feature groups.
    items:
      $ref: ../domain/EtimFeatureGroup.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml