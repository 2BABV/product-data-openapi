type: object
description: |
  Bulk response containing ETIM modelling class-feature-value relations with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM modelling class-feature-value relations.
    items:
      $ref: ../domain/EtimModellingClassFeatureValue.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml