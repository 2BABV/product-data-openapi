type: object
description: |
  Bulk response containing ETIM features with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM features.
    items:
      $ref: ../domain/EtimFeature.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml