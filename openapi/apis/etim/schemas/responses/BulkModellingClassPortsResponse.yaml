type: object
description: |
  Bulk response containing ETIM modelling-class port relations with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM modelling-class port relations.
    items:
      $ref: ../domain/EtimModellingClassPort.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml
