type: object
description: |
  Bulk response containing ETIM groups with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM groups.
    items:
      $ref: ../domain/EtimGroup.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml