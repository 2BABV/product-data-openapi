type: object
description: |
  Response containing all available versions for a class code, each with full details.
required:
  - data
properties:
  data:
    type: array
    description: Array of full class details for each version of the requested class code, ordered by version number descending.
    items:
      $ref: ../domain/EtimClassDetail.yaml
