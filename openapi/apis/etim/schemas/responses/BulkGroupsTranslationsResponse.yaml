type: object
description: |
  Bulk response containing ETIM group translations with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM group translations.
    items:
      $ref: ../domain/EtimTranslation.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml