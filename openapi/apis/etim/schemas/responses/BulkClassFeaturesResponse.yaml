type: object
description: |
  Bulk response containing ETIM class-feature relations with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM class-feature relations.
    items:
      $ref: ../domain/EtimClassFeature.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml