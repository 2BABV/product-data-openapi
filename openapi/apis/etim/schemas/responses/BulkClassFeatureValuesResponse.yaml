type: object
description: |
  Bulk response containing ETIM class-feature-value relations with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM class-feature-value relations.
    items:
      $ref: ../domain/EtimClassFeatureValue.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml