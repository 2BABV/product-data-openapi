type: object
description: |
  Bulk response containing ETIM values with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM values.
    items:
      $ref: ../domain/EtimValue.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml