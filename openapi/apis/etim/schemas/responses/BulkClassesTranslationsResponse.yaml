type: object
description: |
  Bulk response containing ETIM class translations with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM class translations.
    items:
      $ref: ../domain/EtimClassTranslation.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml