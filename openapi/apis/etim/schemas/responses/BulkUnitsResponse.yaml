type: object
description: |
  Bulk response containing ETIM units with cursor-based pagination.
required:
  - data
  - meta
properties:
  data:
    type: array
    description: Array of ETIM units.
    items:
      $ref: ../domain/EtimUnit.yaml
  meta:
    $ref: ../../../../shared/schemas/common/CursorPaginationMetadata.yaml