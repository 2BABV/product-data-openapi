type: object
description: |
  Flat representation of an ETIM class for bulk retrieval.
  Contains the class code, version, group assignment, lifecycle metadata, and release membership.
required:
  - code
  - version
  - revision
  - groupCode
  - status
  - description
  - mutationDate
  - deprecated
  - sectors
  - releases
properties:
  code:
    type: string
    description: ETIM class code.
    pattern: "^EC[0-9]{6}$"
    minLength: 8
    maxLength: 8
    examples:
      - "EC000001"
  version:
    type: integer
    description: Class version number.
    minimum: 1
    examples:
      - 4
  revision:
    type: integer
    description: Revision number within the current version.
    minimum: 0
    examples:
      - 3
  groupCode:
    type: string
    description: Code of the ETIM group this class belongs to.
    pattern: "^EG[0-9]{6}$"
    examples:
      - "EG000001"
  status:
    type: integer
    description: |
      Class lifecycle status code.
      2 = Under construction, 3 = Ready for publication, 5 = Published, 9 = Deleted.
    examples:
      - 5
  description:
    type: string
    description: Class description in ETIM English.
    examples:
      - "Socket outlet"
  definition:
    type: string
    description: Definition or extended description of this class.
    examples:
      - "A device installed in a wall to provide an electrical connection point for plugs"
  mutationDate:
    type: string
    format: date-time
    description: Date and time of last mutation.
    examples:
      - "2024-06-15T10:30:00Z"
  deprecated:
    type: boolean
    description: Whether this class is deprecated (derived from status = 9).
    examples:
      - false
  sectors:
    type: array
    description: |
      Industry sectors this class belongs to.
      E = Electrical, B = Building, M = Mechanical, T = Technical, W = HVAC/Plumbing.
    items:
      type: string
    examples:
      - ["E", "B"]
  successors:
    type: array
    description: Codes of successor classes that replace this one (typically when deprecated).
    items:
      type: string
      pattern: "^EC[0-9]{6}$"
      minLength: 8
      maxLength: 8
    examples:
      - ["EC000123"]
  releases:
    type: array
    description: ETIM releases this class is included in.
    items:
      type: string
    examples:
      - ["ETIM-10.0", "ETIM-9.0", "DYNAMIC"]
examples:
  - code: "EC002714"
    version: 4
    revision: 3
    groupCode: "EG000017"
    status: 5
    description: "Socket outlet"
    mutationDate: "2024-06-15T10:30:00Z"
    deprecated: false
    sectors: ["E", "B"]
    releases: ["ETIM-10.0", "ETIM-9.0", "DYNAMIC"]
