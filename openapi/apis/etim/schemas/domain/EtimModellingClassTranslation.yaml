type: object
description: |
  Translation record for ETIM modelling classes and connection types (MC/CT).
required:
  - classCode
  - classVersion
  - languageCode
  - description
  - mutationDate
properties:
  classCode:
    type: string
    description: ETIM modelling class code (MC = modelling class, CT = connection type).
    pattern: "^(MC|CT)[0-9]{6}$"
    minLength: 8
    maxLength: 8
    examples:
      - "MC000001"
  classVersion:
    type: integer
    description: Class version number.
    minimum: 1
    examples:
      - 2
  languageCode:
    $ref: ./EtimLanguageCode.yaml
  description:
    type: string
    description: Class description in the specified language.
    examples:
      - "Kleplichaam"
  mutationDate:
    type: string
    format: date-time
    description: Date and time of last mutation.
    examples:
      - "2024-06-15T10:30:00Z"
examples:
  - classCode: "MC000001"
    classVersion: 2
    languageCode: "nl-NL"
    description: "Kleplichaam"
    mutationDate: "2024-06-15T10:30:00Z"
  - classCode: "MC000001"
    classVersion: 2
    languageCode: "de-DE"
    description: "Ventilkörper"
    mutationDate: "2024-06-15T10:30:00Z"
