type: object
description: |
  Flat representation of an ETIM feature group for bulk retrieval.
required:
  - code
  - description
  - deprecated
  - successors
  - mutationDate
properties:
  code:
    type: string
    description: ETIM feature group code.
    pattern: "^EFG[0-9]{5}$"
    minLength: 8
    maxLength: 8
    examples:
      - "EFG00001"
  description:
    type: string
    description: Feature group description in ETIM English.
    examples:
      - "General"
  definition:
    type: string
    description: Definition or extended description of this feature group.
    examples:
      - "General characteristics applicable to most product types"
  deprecated:
    type: boolean
    description: Whether this feature group is deprecated.
    examples:
      - false
  successors:
    type: array
    description: Codes of successor feature groups that replace this one (typically when deprecated). Empty when none exist.
    items:
      type: string
      pattern: "^EFG[0-9]{5}$"
      minLength: 8
      maxLength: 8
    examples:
      - ["EFG00099"]
  mutationDate:
    type: string
    format: date-time
    description: Date and time of last mutation.
    examples:
      - "2024-04-10T09:00:00Z"
examples:
  - code: "EFG00001"
    description: "General"
    deprecated: false
    successors: []
    mutationDate: "2024-04-10T09:00:00Z"
