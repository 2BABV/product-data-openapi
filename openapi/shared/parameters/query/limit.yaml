name: limit
in: query
description: Maximum number of items to return per page
required: false
schema:
  type: integer
  default: 100
  minimum: 1
  maximum: 1000
  examples:
    - 10
    - 50
    - 100
